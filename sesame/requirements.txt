# Install with: pip install -r requirements.txt
# Unsloth requires a separate install step — see https://github.com/unslothai/unsloth
# Recommended: use the Colab notebook or run:
#   pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
#   pip install --no-deps trl peft accelerate bitsandbytes

datasets>=2.18.0
torchaudio>=2.2.0
resemblyzer>=0.1.1.dev0
click>=8.1.0
pyyaml>=6.0
tqdm>=4.66.0
numpy>=1.26.0
